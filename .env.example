# Django settings - the values in this file will be used by docker-compose
# Make a copy of this file named .env and fill in the values.

# A secret key for a particular Django installation. This is used to provide cryptographic signing.
SECRET_KEY=your-secret-key-here

# Controls whether Django is in debug mode. Set to 1 for True, 0 for False.
DEBUG=1

# PostgreSQL connection settings
POSTGRES_DB=eth-prediction
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-postgres-password-here

# The full database connection URL. This is constructed from the PostgreSQL settings above.
# The format is: postgres://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgres://postgres:your-postgres-password-here@db:5432/eth-prediction
